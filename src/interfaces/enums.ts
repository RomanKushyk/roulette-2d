export enum AppState {
  splash = 'splash',
  loading = 'loading',
  game = 'game'
}

export enum Modal {
  // settings = 'settings',
  credits = 'credits',
  leaderboard = 'leaderboard',
  // gameOver = 'game-over',
  help = 'help',
}
